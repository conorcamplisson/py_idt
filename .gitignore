# build artifacts — these were tracked until 2026-09-17; nothing generated belongs in git
build/
dist/
*.egg-info/
__pycache__/
*.py[cod]

# built docs — regenerated by sphinx, and hatchling would otherwise sweep them into the sdist
docs/_build/

# environments
.venv/
